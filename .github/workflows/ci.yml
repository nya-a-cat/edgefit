name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    name: Rust (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-14]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Install Rust
        uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # master snapshot
        with:
          toolchain: 1.97.0
      - name: Run tests
        run: cargo test --workspace --locked

  lint:
    name: Rust Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Install Rust lint components
        uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # master snapshot
        with:
          toolchain: 1.97.0
          components: clippy
      - name: Run Clippy
        run: cargo clippy --workspace --all-targets --locked -- --cap-lints warn

  python:
    name: ONNX adapter (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-14]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: tools/onnx-normalize/requirements.txt
      - name: Install ONNX dependency
        run: python -m pip install -r tools/onnx-normalize/requirements.txt
      - name: Run ONNX adapter tests
        run: python -m unittest discover -s tools/onnx-normalize -p "test_*.py"
      - name: Run benchmark orchestration tests
        run: python -m unittest discover -s tools/competitive-benchmark -p "test_*.py"

  python_framework:
    name: Python framework (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-14]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
      - name: Install Rust
        uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # master snapshot
        with:
          toolchain: 1.97.0
      - name: Build native CLI
        run: cargo build --locked --bin edgefit
      - name: Build and install Python framework
        run: python -m pip install ./python
      - name: Verify Python and Rust parity
        run: python -m unittest discover -s python/tests -p "test_*.py"

  action-contract-gate:
    name: Composite Action contract gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Install Rust for the repository-only source path
        uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # master snapshot
        with:
          toolchain: 1.97.0
      - name: Run the local EdgeFit Action
        uses: ./
        env:
          EDGEFIT_ALLOW_SOURCE_BUILD: "1"
        with:
          model: examples/models/good_tiny.edgefit.json
          target: targets/esp32s3.yaml
          report: edgefit.sarif
          summary: edgefit-summary.md
          install-onnx: "false"
      - name: Upload contract-gate reports
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: edgefit-action-contract-gate
          path: |
            edgefit.sarif
            edgefit-summary.md
          if-no-files-found: error

  planner-contract-gate:
    name: Activation planner contract gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
      - name: Install Rust
        uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # master snapshot
        with:
          toolchain: 1.97.0
      - name: Build release CLI
        run: cargo build --workspace --release --locked
      - name: Verify deterministic 10K-node planning
        run: |
          python tools/competitive-benchmark/benchmark.py \
            --manifest tools/competitive-benchmark/planner_scale_manifest.json \
            --edgefit target/release/edgefit \
            --tools edgefit \
            --case-id planner-linear-10000 \
            --edgefit-repetitions 3 \
            --measure-peak-rss \
            --out-dir tmp/planner-contract-gate

  optimizer-contract-gate:
    name: Optimizer contract gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
      - name: Install Rust
        uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # master snapshot
        with:
          toolchain: 1.97.0
      - name: Build release CLI
        run: cargo build --workspace --release --locked
      - name: Verify optimizer 10K pass and fail-closed contracts
        run: |
          python tools/competitive-benchmark/benchmark.py \
            --manifest tools/competitive-benchmark/optimizer_evidence_manifest.json \
            --edgefit target/release/edgefit \
            --tools edgefit \
            --case-id optimizer-linear-10000 \
            --case-id optimizer-no-spill-blocker \
            --edgefit-repetitions 3 \
            --measure-peak-rss \
            --out-dir tmp/optimizer-contract-gate
      - name: Upload optimizer contract evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: edgefit-optimizer-contract-gate
          path: tmp/optimizer-contract-gate/
          if-no-files-found: error

  ci-gate:
    name: ci-gate
    if: ${{ always() }}
    needs: [rust, lint, python, python_framework, action-contract-gate, planner-contract-gate, optimizer-contract-gate]
    runs-on: ubuntu-latest
    steps:
      - name: Enforce required jobs
        env:
          RUST_RESULT: ${{ needs.rust.result }}
          LINT_RESULT: ${{ needs.lint.result }}
          PYTHON_RESULT: ${{ needs.python.result }}
          PYTHON_FRAMEWORK_RESULT: ${{ needs.python_framework.result }}
          ACTION_RESULT: ${{ needs.action-contract-gate.result }}
          PERFORMANCE_RESULT: ${{ needs.planner-contract-gate.result }}
          OPTIMIZER_PERFORMANCE_RESULT: ${{ needs.optimizer-contract-gate.result }}
        run: |
          test "$RUST_RESULT" = success
          test "$LINT_RESULT" = success
          test "$PYTHON_RESULT" = success
          test "$PYTHON_FRAMEWORK_RESULT" = success
          test "$ACTION_RESULT" = success
          test "$PERFORMANCE_RESULT" = success
          test "$OPTIMIZER_PERFORMANCE_RESULT" = success
